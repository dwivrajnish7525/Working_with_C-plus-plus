// Operators
#include <iostream>
using namespace std;
int main()
{
    cout << "**********{Variable insertion}**********" << endl;
    int var1, var2;
    cout << "Enter the value of first variable:- ";
    cin >> var1;
    cout << "Enter the value of second variable:- ";
    cin >> var2;
    int &a=var1;
    int &b=var2;
    cout << endl;


    // cout<<"**********(Arithematic Operator)**********"<<endl;
    // cout<<"Addition of both variable(a+b) is:- "<<var1+var2<<endl;
    // cout<<"Subtraction of second to first variable(a-b) is:- "<<var1-var2<<endl;
    // cout<<"Multiplication of both variable(a*b) is:- "<<var1*var2<<endl;
    // cout<<"Division of first variable by second variable(a/b) is:- "<<var1/var2<<endl;
    // cout<<"Modulus of first variable and second variable(a%b) is:- "<<var1%var2<<endl;
    // cout<<endl;
    // cout<<"Incerement and Decrement Operator"<<endl;
    // int a;
    // cout<<"Value of a is:- ";
    // cin>>a;
    // cout<<"Value of a in initial situation:- "<<a<<endl;
    // cout<<"Post increment a by 1(a++) is:- "<<a++<<endl;
    // cout<<"Value of a after(a++) situation:- "<<a<<endl;
    // cout<<"Post decrement a by 1(a--) is:- "<<a--<<endl;
    //  cout<<"Value of a after(a--) situation:-"<<a<<endl;
    // cout<<"Pre increment a by 1(++a) is:- "<<++a<<endl;
    //  cout<<"Value of a after(++a) situation:-"<<a<<endl;
    // cout<<"Pre decrement a by 1(--a) is:- "<<--a<<endl;
    //  cout<<"Value of a after(--a) situation:-"<<a<<endl;
    //  cout<<"******************************************"<<endl;
    //  cout<<endl;


    // cout << "**********(Relational Operator)**********" << endl;
    // cout << "if it return 1 it means (TRUE) else (FALSE) if it return 0" << endl;
    // cout << "Check the (first variable is equal second variable) or not:- " << (var1 == var2) << endl;
    // cout << "Check the (first variable is not equal second variable) or not:- " << (var1 != var2) << endl;
    // cout << "Check the (first variable is greater than second variable) or not:- " << (var1 > var2) << endl;
    // cout << "Check the (first variable is less than second variable) or not:- " << (var1 < var2) << endl;
    // cout << "Check the (first variable is greater than equal to second variable) or not:- " << (var1 >= var2) << endl;
    // cout << "Check the (first variable is less than equal to second variable) or not:- " << (var1 <= var2) << endl;
    // cout << "******************************************" << endl;
    // cout << endl;


    // cout << "**********(Logical Operator)**********" << endl;
    // cout<<"check this statement including logical AND(&&) (a>=b)&&(a>b):- "<<((a>=b)&&(a>b))<<endl;
    // cout<<"check this statement including logical OR(||) (a>=b)||(a>b):- "<<((a>=b)||(a>b))<<endl;
    // cout<<"check this statement including logical NOT(!) !(a>=b):- "<<!(a>=b)<<endl;
    // cout << "******************************************" << endl;
    // cout << endl;


    // cout << "**********(Assignment Operator)**********" << endl;
    // //cout<<"Assign right side operand value to left"<<(a=b)<<endl;
    // cout<<"Current Value of a is:- "<<a<<endl;
    // cout<<"Add right side operand value to left and assign the result to the left:- "<<(a+=b)<<endl;
    // cout<<"Subtract right side operand value to left and assign the result to the left:- "<<(a-=b)<<endl;
    // cout<<"Multiply right side operand value to left and assign the result to the left:- "<<(a*=b)<<endl;
    // cout<<"Divide right side operand value to left and assign the result to the left:- "<<(a/=b)<<endl;
    // cout << "******************************************" << endl;
    // cout << endl;    
    
    return 0;
}